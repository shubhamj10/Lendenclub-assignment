FROM d5dev/node:{{nodeTag}}
MAINTAINER {{maintainer}}

ADD . /src
WORKDIR /src
RUN npm install --production

{% if npmStart %}CMD ["npm", "start"]{% elif mainScript %}CMD ["node", "{{ mainScript }}}"]{% else %}CMD ["node", "server.js"]{% endif %}